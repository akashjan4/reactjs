import * as React from "react";
import * as ReactDom from "react-dom";

import NamasteComponent from "./components/namaste";

ReactDom.render(<NamasteComponent/>,
      document.getElementById('main'))
